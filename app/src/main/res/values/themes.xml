<?xml version="1.0" encoding="utf-8"?>
<resources>

    <style name="Theme.SolverApp.Base" parent="android:Theme.Material.Light.NoActionBar" />

    <!-- Default app theme. API 31+ adds system splash config in values-v31. -->
    <style name="Theme.SolverApp" parent="Theme.SolverApp.Base" />

    <style name="Theme.SolverApp.Transparent" parent="Theme.SolverApp.Base">
        <item name="android:windowIsTranslucent">true</item>
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:windowContentOverlay">@null</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:backgroundDimEnabled">false</item>
        <item name="android:windowIsFloating">false</item>
    </style>
</resources>
